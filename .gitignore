# OS noise
.DS_Store

# Local Claude Code tooling (preview launch config, etc.)
.claude/

# Sensitive / business documents (order confirmations, invoices)
docs/*.pdf

# Node build artifacts (mcp/ package + Astro site)
node_modules/
dist/
.astro/
*.tsbuildinfo

# Astro build output (the deployable site — run `npm run build` to regenerate, then upload it)
public/
